package loveLetterGui.subJPanelGui;

import javax.swing.JPanel;

public class StartNewGame extends JPanelTemplate{
  private static final String START_GAME_DIALOG = "Press the Next button to start a new game";
  private static final int START_GAME_WIDTH =;
  private static final int START_GAME_HEIGHT =;
  StartNewGame(){
    super(START_GAME_DIALOG, START_GAME_WIDTH, START_GAME_HEIGHT);
  }
}
